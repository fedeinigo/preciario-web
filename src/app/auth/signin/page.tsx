"use client";

import AuthLoginCard from "@/app/components/AuthLoginCard";

/**
 * /auth/signin
 * Muestra el mismo hero de inicio de sesión y
 * sólo permite autenticación con Google.
 */
export default function SignInPage() {
  return <AuthLoginCard />;
}
